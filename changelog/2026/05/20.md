# [2026-05-20]
### Added
- feat(maxos): add LM Studio home-manager module for rig and G16
  - New module at `modules/04-applications/home/development/editors/lmstudio.nix`
  - Registered in `modules/home-layered.nix` under Layer 4 (Applications)
  - Enabled via `maxos.tools.lmstudio.enable = true` in `hosts/rig/home.nix` and `hosts/G16/home.nix`

### Removed
- refactor(maxos): remove OpenClaw integration
  - Deleted `modules/04-applications/home/development/editors/openclaw.nix`
  - Removed `nix-openclaw` flake input from `flake.nix` and `flake.lock`
  - Removed `nix-openclaw` from `lib/host-config.nix` extraSpecialArgs
  - Removed `maxos.tools.openclaw` config from `hosts/rig/home.nix` and `hosts/G16/home.nix`
